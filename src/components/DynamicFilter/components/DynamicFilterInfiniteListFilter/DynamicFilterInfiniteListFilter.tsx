import React, { FC, ReactElement } from 'react'
import { DynamicFilterInfiniteListFilterProps } from '../../utils'
import styles from './DynamicFilterInfiniteListFilter.module.scss'

const DynamicFilterInfiniteListFilter: FC<DynamicFilterInfiniteListFilterProps> = ({
  items,
  key,
  filterItems,
  withFilters,
}): ReactElement => {
  return <div></div>
}

export default DynamicFilterInfiniteListFilter
